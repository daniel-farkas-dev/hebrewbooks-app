import 'package:flutter/material.dart';

var lightTheme = ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: const Color.fromRGBO(252, 88, 5, 1.0), brightness: Brightness.light),
  useMaterial3: true,
);